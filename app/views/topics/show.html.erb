<div class="container">
  <div class="mt-3">
    <%= render "topics/card", topic: @topic %>
  </div>
  <div class="d-flex mt-3">
    <%= link_to "back", chatrooms_path, class: "btn btn-warning rounded-4 w-50" %>
      <%= link_to "Leave", user_topic_path(@current_user_topic), data: { turbo_method: :patch }, class: "btn btn-light rounded-4 w-50" %>
  </div>
  <%= render "topics/chatroom" %>
</div>
